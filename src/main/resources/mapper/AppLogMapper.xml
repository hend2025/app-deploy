<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aeye.app.deploy.mapper.AppLogMapper">

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO t_app_log (app_code, version, log_level, log_content, log_time, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.appCode}, #{item.version}, #{item.logLevel}, #{item.logContent}, #{item.logTime}, #{item.createTime})
        </foreach>
    </insert>

    <select id="selectByAppCode" resultType="com.aeye.app.deploy.model.AppLog">
        SELECT log_id, app_code, version, log_level, log_content, log_time, create_time
        FROM t_app_log
        WHERE app_code = #{appCode}
        <if test="startTime != null">
            AND log_time &gt;= #{startTime}
        </if>
        <if test="endTime != null">
            AND log_time &lt;= #{endTime}
        </if>
        ORDER BY log_time DESC
        LIMIT #{limit}
    </select>

    <delete id="deleteBeforeDate">
        DELETE FROM t_app_log WHERE create_time &lt; #{beforeDate}
    </delete>

</mapper>
